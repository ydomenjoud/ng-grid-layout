import { GridDraggableDirective } from './grid-draggable.directive';

describe('GridDraggableDirective', () => {
  it('should create an instance', () => {
    // const directive = new GridDraggableDirective();
    // expect(directive).toBeTruthy();
  });
});
