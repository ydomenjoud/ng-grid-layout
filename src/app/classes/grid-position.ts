/**
 * Grid Position
 */
export class GridPosition {
  /**
   * row number position ( vertical )
   * @type {number}
   */
  row: number;

  /**
   * col number position ( horizontal )
   * @type {number}
   */
  col: number;


  constructor() {
    this.row = 0;
    this.col = 0;
  }
}
